{
  "rules": {
    "users": {
      "$user": {
        ".read": "auth.uid === $user",
        ".write": "auth.uid === $user",
      },
    ".indexOn": ["tennant"]
    },
    "groups": {
      "$tennant": {
        ".read": true,
        ".indexOn": ["current"]
      }
    }
  }
}
